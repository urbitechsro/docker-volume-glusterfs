version: '3.8'
services: 
  alpine2:
    image: nginx:latest
    working_dir: /data
    volumes:
      - test-volume:/data

volumes:
  test-volume: #Notice the difference in docker volume name from Stack A
    driver: urbitech/glusterfs:8
    driver_opts:
      volname: gv0
      subdir: foo